import React from 'react';
import { View } from 'react-native';
import {Wakai2} from '../../components/indeks';

const wakaii2= ({ onPress}) => {
    return (
        <View>
            <Wakai2 onPress={onPress}/>         
        </View>
    );
};
export default wakaii2;