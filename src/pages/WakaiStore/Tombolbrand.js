import React from 'react';
import { View } from 'react-native';
import {Wakai1} from '../../components/indeks';

const wakaii1= ({ onPress}) => {
    return (
        <View>
            <Wakai1 onPress={onPress}/>         
        </View>
    );
};
export default wakaii1;