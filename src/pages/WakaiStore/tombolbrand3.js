import React from 'react';
import { View } from 'react-native';
import {Wakai3} from '../../components/indeks';

const wakaii3= ({ onPress}) => {
    return (
        <View>
            <Wakai3 onPress={onPress}/>         
        </View>
    );
};
export default wakaii3;