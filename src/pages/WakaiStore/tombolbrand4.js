import React from 'react';
import { View } from 'react-native';
import {Wakai4} from '../../components/indeks';

const wakaii4= ({ onPress}) => {
    return (
        <View>
            <Wakai4 onPress={onPress}/>         
        </View>
    );
};
export default wakaii4;