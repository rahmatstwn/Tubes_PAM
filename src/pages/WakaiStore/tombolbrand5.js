import React from 'react';
import { View } from 'react-native';
import {Wakai5} from '../../components/indeks';

const wakaii5= ({ onPress}) => {
    return (
        <View>
            <Wakai5 onPress={onPress}/>         
        </View>
    );
};
export default wakaii5;