import React from 'react';
import { View } from 'react-native';
import {Ardiless1} from '../../components/indeks';

const Ardiles1 = ({ onPress}) => {
    return (
        <View>
            <Ardiless1 onPress={onPress}/>         
        </View>
    );
};
export default Ardiles1;